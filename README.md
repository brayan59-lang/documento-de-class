# documento-de-class
el html contiene &lt;form>, &lt;input>, &lt;label>, &lt;button>   y capture datos document.getEleme  y el css con magimntById().value redondeo con    Math.round()         margin, padding, border
